import bannerImage from '@/assets/distinctly-ahead-banner-small-text.png'

// Intrinsic pixel size of the hero artwork. Vehicle hotspots are stored as
// fractions of this box, so both the banner plate and the zoomed-in poster
// fallback can be positioned from the same numbers.
export const heroBanner = {
  src: bannerImage,
  width: 2012,
  height: 782,
}
