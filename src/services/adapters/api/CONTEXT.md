# services/adapters/api/ – Phase 2 Real API Adapters

Stub files for Phase 2. Each file exports the same interface as its `mock/` counterpart but will make real HTTP/API calls.

**Phase 1 status:** These files contain only stub exports. Do not implement until Phase 2 API contracts are defined.

**Phase 2 action:** Implement each method to call the corresponding SuccessFactors / internal API endpoint, then update the service file to import from `api/` instead of `mock/`.
